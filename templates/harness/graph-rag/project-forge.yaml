project:
  goal: "Graph RAG with knowledge graph and LLM"
  chosen_stack:
    name: "graph-rag"
    language: "Python"
    runtime: "FastAPI + Neo4j + LangChain"
    rationale: "Entity-relationship Q&A with knowledge graph for complex reasoning."
commands:
  install: "pip install -e ."
  test: "pytest"
  lint: "ruff check ."
  typecheck: "mypy src"
  build: "echo No build step"
  run: "uvicorn src.main:app --reload"
  smoke: "python -c from src.main import app; print(OK)"
harness:
  status: "sample"
  verified: false
