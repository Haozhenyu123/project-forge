project:
  goal: "Full-stack web app with Next.js and Supabase"
  chosen_stack:
    name: "nextjs-supabase"
    language: "TypeScript"
    runtime: "Next.js + Supabase"
    rationale: "Full-stack with Next.js frontend, Supabase backend (PostgreSQL, Auth, Realtime)."
commands:
  install: "npm install"
  test: "npm run test"
  lint: "npm run lint"
  typecheck: "npm run typecheck"
  build: "npm run build"
  run: "npm run dev"
  smoke: "npm run build"
harness:
  status: "sample"
  verified: false
