name: Project Forge CI
on: [push, pull_request]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify project.godot exists
        run: test -f project.godot && echo "Project file OK"
