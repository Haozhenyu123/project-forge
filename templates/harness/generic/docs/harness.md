# Generic Harness

This harness defines the standard Project Forge command contract for projects without a detected language-specific stack.

## How to verify

Replace the generic example commands in `project-forge.yaml` with project-specific commands, then run each command from the project root before handing off the project.
